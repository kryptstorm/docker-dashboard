{{#if page.title}}
<h1>{{page.title}}</h1>
{{/if}}

{{#if page.breadcrumbs}}
<ol class="breadcrumb">
  {{#each page.breadcrumbs}}
    {{#if @last}}
      <li class="active">{{name}}</li>
    {{else}}
      <li><a href="{{url}}">{{name}}</a></li>
    {{/if}} 
  {{/each}}
</ol>
{{/if}}